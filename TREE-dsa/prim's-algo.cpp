// to  find the minimum spaning tree MST we  are using prim's algorithum
// when you converted grah into tree such that it contains N nodes and N-1 edges is called  prims
// every node is  reachable another node
// cycle is not present

#include <iostream>
#include <vector>
using namespace std;
/*
vector<bool> visited(false);
vector<int> key(INT16_MAX);
vector<int> parent(-1);


//  unorderd_map<int,list<pair<int,int>>adj 
for(int i =0;i<n;i++){
  int  u = g[i].first.first;
  int v = g[i].first.second;
  int w = g[i].second;
}

*/