#include <iostream>
#include <vector>
using namespace std;

/*

void printArray(int arr[], int n) {
   cout << "The reversed array is:- " << endl;
   for (int i = 0; i < n; i++) {
      cout << arr[i] << " ";
   }
}
void print(int n[], int size)
{
    int start = 0;
    int end = size - 1;
    while (start < end)
    {
        swap(n[start], n[end]);

        start++;
        end--;
    }
    printArray(n, size);
}

int main()
{
    int arr[5] = {1, 2, 3, 4, 5};
     int size = 5;
    print(arr, 5);

    return 0;
}
*/

