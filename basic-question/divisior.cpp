#include <iostream>
using namespace std;

// find the divisior of n

//  brute force approch  to find the divisior

/*void divisior(int n)
{
    for (int i = 1; i <= n; i++)
    {
        if (n % i == 0)
        {
            cout << i << endl;
        }
    }
}*/

// optimal approch

void divisior(int n)
{
    
}

int main()
{

    int n;
    cout << "enter the n" << endl;
    cin >> n;
    divisior(n);
    return 0;
}