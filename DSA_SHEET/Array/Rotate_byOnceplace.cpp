
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

// rotate the first element and set the last 
/*
int main()
{
    int arr[] = {1, 2, 3, 4, 5, 6};
    int size = 6;
    int temp = arr[0];  // here first element store in the temp variable
    for (int i = 1; i < size; i++)  //using loop  to itreate 
    {
        arr[i - 1] = arr[i];
    }
     arr[size - 1] = temp ;  // add the temp elemnt in last 
    for (int i = 0; i < size; i++)
    {
        cout << arr[i] << " ";
    }

    return 0;
}
*/
